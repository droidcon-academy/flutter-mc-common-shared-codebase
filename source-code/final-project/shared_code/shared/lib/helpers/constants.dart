enum ResponsiveSizes {
  mobile(600.0),
  webDesktopTablet(601.0),
  sidebarWidth(320.0);

  const ResponsiveSizes(this.value);
  final double value;
}

class ImageSizes {
  static const imageHeight = 350.0;
}
